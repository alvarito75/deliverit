import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.upds.deliverit',
  appName: 'deliverit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
